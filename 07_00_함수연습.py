import random

random.randrange(1,6) # 1에서 5까지 난수 발생, 6은 포함안됨.

random.randint(1,5) # 1에서 5까지 난수 발생, 5는 포함

a = [1,2,3,4,5]
random.choice(a)

#random 은 randrange, randint, random.choice 3개를 가장 많이 쓴다.

